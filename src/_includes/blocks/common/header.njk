{% macro headerNavItem(link, title, itemSection) %}
  <a class="inline-block px-3 sm:px-4 py-1.5 text-white rounded-sm {{ 'font-extrabold' if itemSection == tcaTopSection }} hover:bg-zinc-600" href="{{ link | url }}">{{ title }}</a>
{% endmacro %}

<header class="shadow-lg shadow-slate-800 dark:bg-black dark:bg-opacity-75 text-white p-4">
  <div class="tca-container-lg">
    <div class="flex flex-col sm:flex-row gap-y-2 items-center justify-between">
      <a class="text-3xl font-bold" href="{{ '/' | url }}">
        TechAurelian
      </a>

      <nav class="font-medium">
        {# {{ headerNavItem("/", "Home", "home") }} #}
        {{ headerNavItem("/apps/", "Apps & Programs", "apps") }}
        {# {{ headerNavItem("/reviews/", "Reviews", "reviews") }} #}
        {{ headerNavItem("/contact/", "Contact", "contact") }}
        {{ headerNavItem("/about/", "About", "about") }}
        {{ headerNavItem("https://github.com/TechAurelian", "GitHub", "github") }}
      </nav>
    </div>
    {# <ul class="mt-16">
        <li><a href="/helloworldcounters/">Hello World Counters</a></li>
      </ul> #}
  </div>
</header>